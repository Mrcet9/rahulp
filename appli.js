document.getElementById("bus-pass-form").addEventListener("submit", function(event) {
    event.preventDefault(); // Prevent default form submission
    window.location.href = "thankyou.html"; // Redirect to the Thank You page
});
